# C-Simplified-Compiler
A 3-phase compiler for a simplified C-like language built using ANTLR. Includes AST construction, name analysis with optimizations, and type &amp; vulnerability checking (memory leaks, uninitialized variables, etc.).
